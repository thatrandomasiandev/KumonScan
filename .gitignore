node_modules/
.env
.env.*
!.env.example
*.db
*.db-wal
*.db-shm
dist/
.DS_Store

# Confidential / local-only
invoices/
.claude/
.impeccable/

# Roster exports — student PII, never commit
*.csv
*.tsv
.vercel

# Live cross-worktree agent coordination state (ledger + locks), never versioned.
# The single shared copy lives in the primary checkout; see .cursor/rules/multi-agent-protocol.mdc
.agent-coordination/
